# Versiones anteriores (solo archivo)

Páginas HTML que existían únicamente en los paquetes originales descargados y que fueron
**superadas** por las versiones de `02-marketing/` y `03-site-render/` (teléfono corregido,
rutas de assets arregladas, header/footer restaurados).

Se conservan solo como referencia histórica. **No se pueden abrir como sitio**: sus imágenes y
estilos apuntaban a carpetas del paquete original que ya no existen.
