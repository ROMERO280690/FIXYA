# FIXYA — Homepage design render

Prototipo estático (HTML + CSS) de la homepage rediseñada de **FIXYA**.

- Abrí `index.html` en el navegador (sin build step).
- Assets en `assets/` copiados desde `fixya-pack-final/`.
- No es el sitio en producción; es un mockup de diseño / handoff visual.

Marca: navy `#0B1F3A` · naranja `#F5A623` / `#FF8A00` · tipografía Inter + Plus Jakarta Sans.
